# 크림빵

```python
import sys

input = sys.stdin.readline

def solution():
    N, K, P = map(int, input().split())
    breads = input()
    print(sum(breads[i*K*2:(i+1)*K*2].count('0') < P for i in range(N)))

solution()
```

