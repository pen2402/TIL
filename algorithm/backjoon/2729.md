# 이진수 덧셈

```python
import sys

input = sys.stdin.readline

def solution():
    T = int(input())
    for _ in range(T):
        a, b = map(lambda x: int(x, 2), input().split())
        print(bin(a+b)[2:])

solution()
```

