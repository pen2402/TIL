# 관공... 어찌하여 목만 오셨소...

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    for _ in range(N):
        name = input().rstrip()
        if name.find('S') >= 0:
            print(name)
            return

solution()
```

