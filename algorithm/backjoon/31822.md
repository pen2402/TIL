# 재수강

```python
import sys

input = sys.stdin.readline

def solution():
    code = input()[:5]
    N = int(input())
    res = 0
    for _ in range(N):
        res += input().startswith(code)
    print(res)

solution()
```

