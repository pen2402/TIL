# 숫자

```python
import sys

input = sys.stdin.readline

def solution():
    A, B = map(int, input().split())
    if A > B:
        A, B = B, A
    print(max(B-A-1, 0))
    print(*range(A+1, B))

solution()
```

