# 럭비 클럽

```python
import sys

input = sys.stdin.readline

def solution():
    while 1:
        name, a, w = input().split()
        a, w = map(int, (a, w))
        if a == 0:
            return
        print(name, 'Senior' if a > 17 or w >= 80 else 'Junior')

solution()
```

