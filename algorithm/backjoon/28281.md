# 선물

```python
import sys

input = sys.stdin.readline

def solution():
    N, X = map(int, input().split())
    res = prev = 2000
    for a in map(int, input().split()):
        if prev+a < res:
            res = prev+a
        prev = a
    res *= X
    print(res)

solution()
```

