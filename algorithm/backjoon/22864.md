# 피로도

```python
import sys

input = sys.stdin.readline

def solution():
    A, B, C, M = map(int, input().split())
    res = f = 0
    for _ in range(24):
        if f+A <= M:
            f += A
            res += B
        else:
            f = max(f-C, 0)
    print(res)

solution()
```

