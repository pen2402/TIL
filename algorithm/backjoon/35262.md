# 지금부터 완장 찬양을 시작하겠습니다

```python
import sys

input = sys.stdin.readline

def solution():
    N, K = map(int, input().split())
    S = input()
    print(0 if S.find('0'*K) >= 0 else 1)

solution()
```

