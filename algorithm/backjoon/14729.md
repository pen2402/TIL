# 칠무해

```python
import sys, heapq

input = sys.stdin.readline

N = int(input())
scores = []
for _ in range(N):
    heapq.heappush(scores, float(input()))
for _ in range(7):
    score = heapq.heappop(scores)
    print(f'{score:.3f}')
```

