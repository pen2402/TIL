# 등차수열에서 항 번호 찾기

```python
import sys

input = sys.stdin.readline

def solution():
    a, d, k = map(int, input().split())
    print((k-a)//d+1 if (k-a)%d == 0 and (k-a)//d >= 0 else 'X')

solution()
```

