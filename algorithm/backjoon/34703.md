# 공강 사수

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    days = set()
    for _ in range(N):
        d = input().rstrip()
        days.add(d)
    print('YES' if len(days) < 5 else 'NO')

solution()
```

