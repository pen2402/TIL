# Dedupe

```python
import sys

input = sys.stdin.readline

def solution():
    T = int(input())
    for _ in range(T):
        S = input().rstrip()
        res = ''
        last = ''
        for c in S:
            if last != c:
                res += c
            last = c
        print(res)

solution()
```

