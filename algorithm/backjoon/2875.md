# 대회 or 인턴

```python
import sys

input = sys.stdin.readline

def solution():
    N, M, K = map(int, input().split())
    cnt = min(N//2, M)
    d = max(K-N-M+cnt*3, 0)
    print(cnt-d//3-(d%3 > 0))

solution()
```

