# 찬반투표

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    votes = tuple(map(int, input().split()))
    if votes.count(0) >= (N+1)//2:
        print('INVALID')
    else:
        print('APPROVED' if sum(votes) > 0 else 'REJECTED')

solution()
```

