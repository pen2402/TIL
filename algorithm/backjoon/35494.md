# 맛있는 햄버거

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    V = max_r = 0
    for r in map(int, input().split()):
        if r > max_r:
            max_r = r
        V += max_r
    print('Oh My God!' if V%3 else 'Delicious!')

solution()
```

