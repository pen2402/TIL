# 빵

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    res = -1
    for _ in range(N):
        A, B = map(int, input().split())
        if A > B:
            continue
        if res < 0 or B < res:
            res = B
    print(res)

solution()
```

