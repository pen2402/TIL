# 카드 구매하기

```python
N = int(input())
p = list(map(int, input().split()))
p_per = [p[i-1]/i for i in range(1, N+1)]
total = 0
while N > 0:
    max_val = p_per.index(max(p_per))
    total += p[max_val]
    N -= (max_val+1)
    p_per = p_per[:N]
print(total)
```

