# 좌표 압축

```python
import sys
N = int(sys.stdin.readline())
l = list(map(int, sys.stdin.readline().split()))
s = sorted(set(l))
dic = {s[i] : i for i in range(len(s))}
for n in l:
    print(dic[n], end=' ')
```

