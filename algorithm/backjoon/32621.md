# 동아리비 횡령

```python
import sys

input = sys.stdin.readline

def solution():
    s = input().rstrip()
    if '+' in s and s[0] != '0' and s.split('+')[0].isdigit() and len(s.split('+')) == 2 and len(set(s.split('+'))) == 1:
        print('CUTE')
    else:
        print('No Money')

solution()
```

