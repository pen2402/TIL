# 짝수를 찾아라

```python
import sys

input = sys.stdin.readline

def solution():
    T = int(input())
    for _ in range(T):
        numbers = tuple(n for n in map(int, input().split()) if n%2 == 0)
        print(sum(numbers), min(numbers))

solution()
```

