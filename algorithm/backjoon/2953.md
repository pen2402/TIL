# 나는 요리사다

```python
import sys

input = sys.stdin.readline

def solution():
    num = max_total = 0
    for i in range(1, 6):
        total = sum(map(int, input().split()))
        if total > max_total:
            num = i
            max_total = total
    print(num, max_total)

solution()
```

