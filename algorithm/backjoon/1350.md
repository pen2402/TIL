# 진짜 공간

```python
import sys

input = sys.stdin.readline

def solution():
    input()
    files = tuple(map(int, input().split()))
    cluster = int(input())
    res = 0
    for file in files:
        res += (file//cluster+(file%cluster > 0))*cluster
    print(res)

solution()
```

