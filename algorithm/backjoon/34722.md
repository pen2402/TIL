# 출제자가 몇 명

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    res = 0
    for _ in range(N):
        s, c, a, r = map(int, input().split())
        res += s >= 1000 or c >= 1600 or a >= 1500 or 30 >= r > 0
    print(res)

solution()
```

