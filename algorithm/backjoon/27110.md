# 특식 배부

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    A, B, C = map(int, input().split())
    res = min(A, N)+min(B, N)+min(C, N)
    print(res)

solution()
```

