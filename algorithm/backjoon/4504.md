# 배수 찾기

```python
import sys

input = sys.stdin.readline

def solution():
    n = int(input())
    while 1:
        num = int(input())
        if num == 0:
            return
        print(f"{num} is {'NOT ' if num%n else ''}a multiple of {n}.")

solution()
```

