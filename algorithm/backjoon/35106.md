# 릴레이 가위바위보 게임

```python
import sys

input = sys.stdin.readline

def solution():
    N = int(input())
    cnt_list = [0]*4
    for num in map(int, input().split()):
        cnt_list[num] += 1
    print(cnt_list.index(N-1, 1))
    print(cnt_list.index(N+1, 1))

solution()
```

