# 입양 시각 구하기(1)

```sql
SELECT HOUR, COUNT(HOUR) FROM (SELECT TO_CHAR(DATETIME, 'HH24') AS HOUR FROM ANIMAL_OUTS WHERE TO_CHAR(DATETIME, 'HH24') BETWEEN '09' AND '20') GROUP BY HOUR ORDER BY HOUR;
```

