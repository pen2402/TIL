# 물고기 종류 별 대어 찾기

```mysql
SELECT A.ID, B.FISH_NAME, A.LENGTH
FROM FISH_INFO A, FISH_NAME_INFO B
WHERE A.FISH_TYPE = B.FISH_TYPE AND A.LENGTH = (
    SELECT MAX(LENGTH)
    FROM FISH_INFO
    WHERE FISH_TYPE = A.FISH_TYPE
    GROUP BY FISH_TYPE
)
ORDER BY A.ID ASC;
```

